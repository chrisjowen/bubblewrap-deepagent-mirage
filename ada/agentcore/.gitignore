# Secrets (local environment files are never committed)
.env.local

# CDK Build Artifacts
cdk/cdk.out/
cdk/node_modules/

# CLI Internals
.cli/*

# Ephemeral Staging
.cache/*

# Deploy artifacts + AWS account-specific state (public repo — keep account IDs local)
ada.zip
aws-targets.json
